{
  "id": "ai.github_query",
  "node_type": "AI",
  "tool_key": null,
  "service": "github",
  "tags": ["github", "깃허브", "pr", "pull request", "이슈", "issue", "리포", "repo", "커밋"],
  "menu": "GitHub 조회(PR/이슈/리포): tools는 빈 배열, 전용 서브에이전트가 자동 부착",
  "fixed": {
    "type": "AI",
    "config": {
      "agentType": "react",
      "credentialId": "",
      "tools": []
    }
  },
  "slots": [
    {"name": "label", "path": "label", "required": true, "kind": "string", "description": "노드 라벨"},
    {"name": "llmProvider", "path": "config.llmProvider", "required": true, "kind": "provider", "description": "요청자 provider 계승"},
    {"name": "prompt", "path": "config.prompt", "required": true, "kind": "string", "description": "GitHub 작업 지시(owner/repo 명시). 목록 조회는 단일 페이지·개수 상한 못박고 후속 노드가 쓰는 필드만 추출하도록 지시."}
  ],
  "allowed_config_fields": ["agentType", "credentialId", "tools", "llmProvider", "prompt", "systemMessage"],
  "golden_snippet": {
    "id": "node-2",
    "type": "AI",
    "label": "GitHub PR 목록 조회",
    "config": {
      "llmProvider": "CLAUDE",
      "credentialId": "",
      "prompt": "team-ieum/ieum-backend의 PR을 최신순 1페이지(per_page=30, page=1, sort=created, direction=desc)만 조회하고, 최근 7일 내 생성된 것만 number, title, html_url, created_at 필드만 JSON 배열로 반환해줘. 값은 임의로 요약/변경하지 마.",
      "agentType": "react",
      "tools": []
    }
  }
}
