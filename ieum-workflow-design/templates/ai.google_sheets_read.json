{
  "id": "ai.google_sheets_read",
  "node_type": "AI",
  "tool_key": "builtin:google_sheets_read",
  "tags": ["sheets", "스프레드시트", "시트", "구글 시트", "google sheets", "읽기", "조회"],
  "menu": "Google Sheets 읽기 (spreadsheet_id, range 필요)",
  "fixed": {
    "type": "AI",
    "config": {
      "serviceType": "GOOGLE",
      "agentType": "react",
      "credentialId": "",
      "tools": [{"name": "builtin:google_sheets_read"}]
    }
  },
  "slots": [
    {"name": "label", "path": "label", "required": true, "kind": "string", "description": "노드 라벨"},
    {"name": "description", "path": "description", "required": true, "kind": "string", "description": "이 노드가 무슨 일을 하는지 일반 사용자에게 알려주는 자연어 1문장. 도구 키·필드명·변수 참조식 같은 기술 용어 없이, 사용자가 얻는 결과 중심으로 쓴다."},
    {"name": "llmProvider", "path": "config.llmProvider", "required": true, "kind": "provider", "description": "요청자 provider 계승"},
    {"name": "model", "path": "config.model", "required": true, "kind": "model", "description": "요청 provider의 기본 모델. 시스템이 자동 주입한다(작성 금지)."},
    {"name": "prompt", "path": "config.prompt", "required": true, "kind": "string", "description": "읽기 지시. spreadsheet_id와 range 명시. 후속 노드가 쓰는 컬럼만 추출하도록 지시."}
  ],
  "allowed_config_fields": ["agentType", "credentialId", "tools", "llmProvider", "prompt", "systemMessage", "model", "serviceType"],
  "golden_snippet": {
    "id": "node-2",
    "type": "AI",
    "label": "시트 데이터 읽기",
    "description": "구글 시트에 적힌 내용을 읽어와요.",
    "config": {
      "llmProvider": "GEMINI",
      "credentialId": "",
      "prompt": "spreadsheet_id 'sheet-abc123'의 range 'Sheet1!A:B'를 읽어 행 목록을 JSON 배열로 반환해줘.",
      "agentType": "react",
      "tools": [{"name": "builtin:google_sheets_read"}]
    }
  }
}
