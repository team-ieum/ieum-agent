{
  "id": "ai.notion_append_block",
  "node_type": "AI",
  "tool_key": "builtin:notion_append_block",
  "tags": ["notion", "노션", "블록 추가", "append", "본문 추가", "댓글"],
  "menu": "Notion 블록 추가 (기존 페이지에 내용 덧붙이기, page_id 필요)",
  "fixed": {
    "type": "AI",
    "config": {
      "agentType": "react",
      "credentialId": "",
      "tools": [{"name": "builtin:notion_append_block"}]
    }
  },
  "slots": [
    {"name": "label", "path": "label", "required": true, "kind": "string", "description": "노드 라벨"},
    {"name": "description", "path": "description", "required": true, "kind": "string", "description": "이 노드가 무슨 일을 하는지 일반 사용자에게 알려주는 자연어 1문장. 도구 키·필드명·변수 참조식 같은 기술 용어 없이, 사용자가 얻는 결과 중심으로 쓴다."},
    {"name": "llmProvider", "path": "config.llmProvider", "required": true, "kind": "provider", "description": "요청자 provider 계승"},
    {"name": "prompt", "path": "config.prompt", "required": true, "kind": "string", "description": "추가 지시. page_id와 추가할 블록 내용 명시. 내용은 선행 노드 참조식 사용."}
  ],
  "allowed_config_fields": ["agentType", "credentialId", "tools", "llmProvider", "prompt", "systemMessage"],
  "golden_snippet": {
    "id": "node-3",
    "type": "AI",
    "label": "Notion 블록 추가",
    "description": "노션 페이지 아래에 내용을 이어서 적어요.",
    "config": {
      "llmProvider": "CLAUDE",
      "credentialId": "",
      "prompt": "page_id '1a2b3c4d'에 다음 내용을 마크다운 블록으로 추가해줘: {{nodes.node-2.output.output}}",
      "agentType": "react",
      "tools": [{"name": "builtin:notion_append_block"}]
    }
  }
}
