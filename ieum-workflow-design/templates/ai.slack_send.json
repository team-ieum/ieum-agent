{
  "id": "ai.slack_send",
  "node_type": "AI",
  "tool_key": "slack",
  "tags": ["slack", "슬랙", "메시지", "알림", "채널", "notify"],
  "menu": "Slack 메시지 전송 (webhookCredentialId는 보유 웹훅에서 선택)",
  "fixed": {
    "type": "AI",
    "config": {
      "agentType": "react",
      "credentialId": "",
      "tools": [{"name": "slack"}]
    }
  },
  "slots": [
    {"name": "label", "path": "label", "required": true, "kind": "string", "description": "노드 라벨"},
    {"name": "description", "path": "description", "required": true, "kind": "string", "description": "이 노드가 무슨 일을 하는지 일반 사용자에게 알려주는 자연어 1문장. 도구 키·필드명·변수 참조식 같은 기술 용어 없이, 사용자가 얻는 결과 중심으로 쓴다."},
    {"name": "llmProvider", "path": "config.llmProvider", "required": true, "kind": "provider", "description": "요청자 provider 계승"},
    {"name": "prompt", "path": "config.prompt", "required": true, "kind": "string", "description": "발송 지시문 + 보낼 내용 참조를 함께 작성(참조식 단독 금지). AI 결과는 {{nodes.<id>.output.output}}로 참조. 예: '다음 내용을 슬랙으로 보내줘: {{nodes.node-3.output.output}}'. 채널/웹훅 여러 개면 CLARIFICATION."},
    {"name": "webhookCredentialId", "path": "config.tools.0.config.webhookCredentialId", "required": false, "kind": "string", "description": "보유 Slack 웹훅 자격증명 ID(가용 목록에서 선택). 미상이면 비움."}
  ],
  "allowed_config_fields": ["agentType", "credentialId", "tools", "llmProvider", "prompt", "systemMessage"],
  "golden_snippet": {
    "id": "node-4",
    "type": "AI",
    "label": "Slack 알림 발송",
    "description": "정리된 내용을 슬랙으로 보내드려요.",
    "config": {
      "llmProvider": "CLAUDE",
      "credentialId": "",
      "prompt": "다음 PR 요약을 개발 채널에 슬랙 메시지로 보내줘: {{nodes.node-3.output.prSummary}}",
      "agentType": "react",
      "tools": [{"name": "slack"}]
    }
  }
}
