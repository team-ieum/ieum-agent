{
  "id": "trigger.webhook",
  "node_type": "TRIGGER",
  "tool_key": null,
  "tags": ["웹훅", "webhook", "외부 이벤트", "수신", "콜백", "트리거"],
  "menu": "WEBHOOK 트리거: 외부 이벤트 수신 시 실행",
  "fixed": {
    "type": "TRIGGER",
    "config": {
      "triggerType": "WEBHOOK"
    }
  },
  "slots": [
    {"name": "label", "path": "label", "required": true, "kind": "string", "description": "트리거 라벨 (예: '웹훅 수신 트리거')"},
    {"name": "description", "path": "description", "required": true, "kind": "string", "description": "이 노드가 무슨 일을 하는지 일반 사용자에게 알려주는 자연어 1문장. 도구 키·필드명·변수 참조식 같은 기술 용어 없이, 사용자가 얻는 결과 중심으로 쓴다."}
  ],
  "allowed_config_fields": ["triggerType"],
  "golden_snippet": {
    "id": "node-1",
    "type": "TRIGGER",
    "label": "웹훅 수신 트리거",
    "description": "외부에서 신호가 오면 자동으로 시작돼요.",
    "config": {"triggerType": "WEBHOOK"}
  }
}
